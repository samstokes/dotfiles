#!/bin/bash
action="$1"

set -ueo pipefail

case "$action" in
  start)
    mount-luks
    $HOME/bin/dropbox start -i
    ;;
  stop)
    $HOME/bin/dropbox stop || echo "Oops, couldn't stop Dropbox!" >&2
    umount-luks
    ;;
  *)
    echo "Must pass 'start' or 'stop'"
    exit 1
    ;;
esac
